// This file was generated by Mendix Studio Pro.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package scheduler.actions;

import java.util.Date;
import java.util.List;
import scheduler.impl.ScheduleManager;
import scheduler.proxies.ActionType;
import scheduler.proxies.JobStatus;
import scheduler.proxies.ScheduledAction;
import com.mendix.core.Core;
import com.mendix.core.CoreException;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.systemwideinterfaces.core.IMendixObject;
import com.mendix.webui.CustomJavaAction;

public class EvaluateInstruction extends CustomJavaAction<java.lang.Boolean>
{
	private IMendixObject __JobInstruction;
	private scheduler.proxies.JobInstruction JobInstruction;

	public EvaluateInstruction(IContext context, IMendixObject JobInstruction)
	{
		super(context);
		this.__JobInstruction = JobInstruction;
	}

	@java.lang.Override
	public java.lang.Boolean executeAction() throws Exception
	{
		this.JobInstruction = __JobInstruction == null ? null : scheduler.proxies.JobInstruction.initialize(getContext(), __JobInstruction);

		// BEGIN USER CODE

		IContext sysContext = Core.createSystemContext();
		ScheduledAction action = this.JobInstruction.getJobInstruction_ScheduledAction(sysContext);
		if ( action == null && this.JobInstruction.getActionId() != null ) {
			
			List<IMendixObject> result = Core.createXPathQuery( 
					String.format("//%s[%s=$actionid]", ScheduledAction.entityName,
					ScheduledAction.MemberNames.InternalId.toString())
					).setVariable("actionid", this.JobInstruction.getActionId()).execute(sysContext);;

			if ( result.size() > 0 ) {
				action = ScheduledAction.initialize(getContext(), result.get(0));
				this.JobInstruction.setJobInstruction_ScheduledAction(sysContext, action);
			}
		}

		this.JobInstruction.setStartedOn(sysContext, new Date());
		this.JobInstruction.setStatus(sysContext, JobStatus.Started);
		Core.commit(sysContext, this.__JobInstruction);

		boolean success = false;
		try {
			if ( action == null && this.JobInstruction.getActionType() != ActionType.RestartScheduler ) {
				this.JobInstruction.setFinishedOn(sysContext, new Date());
				this.JobInstruction.setStatus(sysContext, JobStatus.Completed_Failed);
				this.JobInstruction.setDetailedStatus("No action provided for the instruction");
				Core.commit(sysContext, this.__JobInstruction);
			}
			else if ( this.JobInstruction.getActionType() != null ) {
				this.JobInstruction.setFinishedOn(sysContext, new Date());

				switch (this.JobInstruction.getActionType()) {
				case EvaluateRunning:
				case Start:
					ScheduleManager.getInstance().startAction(action);
					success = true;
					break;
				case Stop:
					ScheduleManager.getInstance().stopAction(action);
					success = true;
					break;
				case UpdateConfiguration:
					success = ScheduleManager.getInstance().rescheduleAction(action);
					if ( !success )
						this.JobInstruction
								.setDetailedStatus("Unable to re-schedule the Job, mostlikely there wasn't an active scheduler.");
					break;
				case RestartScheduler:
					success = ScheduleManager.getInstance().restartScheduler();
					break;
				default:
					throw new CoreException("Unknown ActionType");
				}
			}
			else {
				this.JobInstruction.setStatus(sysContext, JobStatus.Completed_Failed);
				this.JobInstruction.setDetailedStatus("No action type specified");
			}
		}
		catch( Exception e ) {
			this.JobInstruction.setStatus(sysContext, JobStatus.Completed_Failed);
			this.JobInstruction.setDetailedStatus("An error occured while processing the request: " + e.getMessage());
			throw e;
		}
		finally {
			if ( success )
				this.JobInstruction.setStatus(sysContext, JobStatus.Completed_Successfull);
			else
				this.JobInstruction.setStatus(sysContext, JobStatus.Completed_Failed);

			this.JobInstruction.setFinishedOn(sysContext, new Date());
			Core.commit(sysContext, this.__JobInstruction);
		}

		return success;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@java.lang.Override
	public java.lang.String toString()
	{
		return "EvaluateInstruction";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
